from datetime import datetime


# Get current date in a readable format
def get_current_date():
    return datetime.now().strftime("%B %d, %Y")


query_writer_instructions = """你的目标是生成高级且多样化的网页搜索查询。这些查询将用于一个能够分析复杂结果、跟踪链接并综合信息的高级自动化网页研究工具。

说明：
- 始终优先生成单条搜索查询；仅在原始问题涉及多个方面或元素，且一条查询不足以覆盖全部内容时，才增加查询条数。
- 每条查询应聚焦于原始问题的一个具体方面。
- 不要生成超过 {number_queries} 条查询。
- 查询应具有多样性；如果主题范围较广，应生成多条查询。
- 不要生成多个类似的查询；一条就够了。
- 查询应确保获取到最新的信息。当前日期为 {current_date}。

格式：
- 请将你的回答格式化为一个 JSON 对象，包含以下两个准确的键名：
   - "rationale"：简要说明这些查询为何与研究主题相关
   - "query"：一个搜索查询组成的列表

示例：

Topic: 去年苹果的股票收入增长更快，还是购买 iPhone 的人数增长更快？
json
{{
    "rationale": "为了准确回答这一对比增长的问题，我们需要关于苹果公司股票表现和 iPhone 销售数据的具体信息。这些查询精准地定位了所需的财务数据：公司收入趋势、具体产品的销量变化，以及在同一财年内的股票价格走势，以便进行直接比较。",
    "query": ["Apple 2024 财年总收入增长", "iPhone 2024 财年销量增长", "Apple 2024 财年股价增长"]
}}


Context: {research_topic}
"""


web_searcher_instructions = """请针对“{research_topic}”进行有针对性的 Google 搜索，收集最新、可信的信息，并将其整合成一份可验证的文本材料。

说明：
- 搜索查询应确保获取到**最新的信息**。当前日期为 {current_date}。
- 应进行**多次、具有多样性**的搜索，以获取全面的信息。
- 在整合关键信息时，务必**准确记录每条信息对应的来源**。
- 输出应为一份**撰写良好的总结或报告**，内容基于你的搜索发现。
- **仅包含搜索结果中找到的信息**，**不得杜撰或补充虚构内容**。

研究主题：  
{research_topic}

"""

reflection_instructions = """你是一位研究助理专家，正在分析关于“{research_topic}”的摘要内容。

说明：
- 请识别摘要中存在的**知识空白或需要深入探究的领域**，并据此生成**后续查询**（可以是一条或多条）。
- 如果摘要内容**已经足以回答用户的问题**，则**不要生成后续查询**。
- 如果存在知识空白，请生成一个**有助于扩展理解的后续查询**。
- 重点关注摘要中**未完全覆盖的技术细节、实现方式或新兴趋势**等方面。

要求：
- 后续查询必须是**自洽的独立问题**，具备明确的上下文，适合用于网页搜索。

输出格式：
- 请将你的回答格式化为一个 JSON 对象，包含以下三个准确的键名：
   - `"is_sufficient"`：true 或 false
   - `"knowledge_gap"`：简要描述缺失的信息或需要澄清的内容（若为 true 则留空）
   - `"follow_up_queries"`：为弥补该空白而提出的具体问题（若为 true 则为空数组）

示例：
json
{{
    "is_sufficient": true,
    "knowledge_gap": "",
    "follow_up_queries": []
}}

或：
json
{{
    "is_sufficient": false,
    "knowledge_gap": "摘要中缺少关于该技术在实际部署中的性能指标和基准测试信息",
    "follow_up_queries": ["用于评估该技术的典型性能基准和测试指标有哪些？"]
}}


请认真分析下方摘要内容，识别知识空白，并按上述 JSON 格式输出：

摘要内容：  
{summaries}

"""

answer_instructions = """请根据下方摘要内容，为用户的问题生成一个**高质量的答案**。

说明：
- 当前日期为 {current_date}。
- 你是一个研究流程中的最后输出环节（**无需在回答中提及这一点**）。
- 你已完整掌握前面所有研究步骤中收集到的信息。
- 你已经获得了用户的问题。
- 请基于下方提供的摘要和用户的问题，撰写**清晰、准确、结构完整的最终答案**。
- 在回答中务必使用**Markdown 格式**正确标注来源，例如：[apnews](https://vertexaisearch.cloud.google.com/id/1-0)，**这一点必须执行**。

用户问题主题：  
{research_topic}

摘要内容：  
{summaries}
"""